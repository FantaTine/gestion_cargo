/* import {cargaison} from './cargaison.js';

export class Aerienne extends cargaison {
    constructor(code: number, poids: number, datedepart: number, datearrivee: number, type: string, distance: number, lieudepart: string, lieuarrivee: string) {
        super(code, poids, datedepart, datearrivee, type, distance, lieudepart, lieuarrivee);
    }
} */